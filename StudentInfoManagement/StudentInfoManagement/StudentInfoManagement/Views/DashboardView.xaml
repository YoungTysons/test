<UserControl x:Class="StudentInfoManagement.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800" Background="Transparent">

    <UserControl.Resources>
        <Style x:Key="DashboardCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="0,0,20,0"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" BlurRadius="10" Opacity="0.05"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <TextBlock Text="Dashboard" FontSize="32" FontWeight="Bold" Foreground="#111827"/>
            <TextBlock Text="Welcome to the Student Information System" FontSize="16" Foreground="#6B7280" Margin="0,5,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource DashboardCard}">
                <StackPanel>
                    <Grid>
                        <TextBlock Text="Total Students" FontSize="14" Foreground="#111827"/>
                        <TextBlock Text="&#xE77B;" FontFamily="Segoe MDL2 Assets" HorizontalAlignment="Right" Foreground="#1D4ED8"/>
                    </Grid>
                    <TextBlock Text="1,234" FontSize="32" FontWeight="Bold" Margin="0,15,0,0" Foreground="#111827"/>
                    <TextBlock Text="Active students" FontSize="12" Foreground="#6B7280"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource DashboardCard}">
                <StackPanel>
                    <Grid>
                        <TextBlock Text="Total Courses" FontSize="14" Foreground="#111827"/>
                        <TextBlock Text="&#xE82D;" FontFamily="Segoe MDL2 Assets" HorizontalAlignment="Right" Foreground="#1D4ED8"/>
                    </Grid>
                    <TextBlock Text="42" FontSize="32" FontWeight="Bold" Margin="0,15,0,0" Foreground="#111827"/>
                    <TextBlock Text="Available courses" FontSize="12" Foreground="#6B7280"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource DashboardCard}" Margin="0">
                <StackPanel>
                    <Grid>
                        <TextBlock Text="Completion Rate" FontSize="14" Foreground="#111827"/>
                        <TextBlock Text="&#xE9D9;" FontFamily="Segoe MDL2 Assets" HorizontalAlignment="Right" Foreground="#1D4ED8"/>
                    </Grid>
                    <TextBlock Text="87%" FontSize="32" FontWeight="Bold" Margin="0,15,0,0" Foreground="#111827"/>
                    <TextBlock Text="Average completion" FontSize="12" Foreground="#6B7280"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>